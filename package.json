{
  "name": "ts-artifact-uploader",
  "version": "1.0.0",
  "description": "upload an artifact",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --platform=node --target=node20 --outfile=dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/artifact": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "esbuild": "^0.27.1",
    "typescript": "^5.9.3"
  }
}
